# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'
project 'XCUILogTest/XCUILogTest.xcodeproj'

target 'XCUILogTest' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for XCUILogTest

  target 'XCUILogTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'XCUILogTestUITests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Swifter', '~> 1.4.7'
  end

end
